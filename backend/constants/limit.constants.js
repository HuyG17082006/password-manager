const FIFTEEN_MINUTES = {
    in_seconds: 15 * 60,
    in_milliseconds: 15 * 60 * 1000,
    in_minutes: 15
}

const ONE_HOUR = {
    in_seconds: 60 * 60,
    in_milliseconds: 60 * 60 * 1000,
    in_minutes: 60
}

const A_DAY = {
    in_seconds: 24 * 60 * 60,
    in_milliseconds: 24 * 60 * 60 * 1000,
    in_minutes: 24 * 60
}

export {
    FIFTEEN_MINUTES,
    ONE_HOUR,
    A_DAY
}